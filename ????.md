# 安徽淮北杜集经济开发区 · 重点项目专班智慧化管理平台

> 用一块大屏替代“Excel → PDF → 图片 → 循环播放”的老办法。
> 项目全生命周期（签约—注册—备案—能评—环评—开工—纳统—入规）一屏呈现，数据可自行维护、随时更新。

---

## 一、怎么看（大屏端）

1. 双击 `index.html`（推荐 Edge / Chrome 浏览器打开）。
2. 按 `F11` 全屏，适合一楼大厅大屏循环播放。
3. 1920×1080 为最佳，其他分辨率会自动等比缩放，不会变形。

**大屏内容说明**

| 位置 | 内容 |
| --- | --- |
| 顶部 | 园区名称、旋转园区徽标、平台标题、实时日期时间 |
| 顶部数字条 | 专班项目数、已办结手续、手续办结率、需协调问题 |
| 左栏上 | 项目结构（已投产 / 建设中 / 前期手续 环形统计） |
| 左栏中 | 手续办理进度（投资协议、工商注册、立项备案、能评、环评、施工许可、开工、纳统、入规 的办结率） |
| 左栏下 | **在办项目动态**：逐个项目循环展示项目名称、投资方、在淮注册公司、联系人、电话、在办节点、进展、下周计划、现场照片 |
| 中间 | **土地利用现状图**（已按大屏科技风重新配色，保留原有地块形状与编号）+ 扫描光带 |
| 中上条 | 当前项目的名称、投资方、联系人、手续办结数、状态 |
| 右栏上 | **专班项目名录**：9 个项目按序号循环高亮 |
| 右栏中 | 当前项目的**节点推进时间轴**（已办结 / 在办、计划日期、进展说明） |
| 右栏下 | 需协调解决问题（来自台账“提示函”栏） |
| 底部 | 项目名称跑马灯 + 状态 |

**交互**：左右两栏每 8 秒自动切换一个项目，**左侧动态卡、右侧名录高亮、时间轴、顶部信息条四处联动**。鼠标点击右侧名录中的任意一行，可立即切到该项目（并重新计时）。

---

## 二、怎么改（管理端）

双击 `admin.html`，输入操作口令（默认 `duji2026`，进入后可修改）。

1. **基础信息**：序号、项目全称、地图上的简称、产业标签、投资方、在淮注册公司、联系人、联系电话。留空“状态”时，系统按规则自动判定。
2. **手续节点**：可增、删、上移、下移每个节点，填写计划完成日期、是否办结、责任部门、负责人、进展情况、下周推进计划。**“需协调解决的问题”一栏填了内容，就会自动出现在大屏右下角的“需协调解决问题”栏。**
3. **现场照片**：可上传多张项目现场图（自动压缩），大屏会随项目循环展示。
4. 编辑完成后有三种保存方式：

| 按钮 | 作用 | 适用场景 |
| --- | --- | --- |
| 保存到本机 | 存进本机浏览器，本机打开的大屏立即生效 | 先在本机验收效果 |
| 导出 data.json / data.js | 下载数据文件 | 手工上传到网站目录（线上更新） |
| 发布到代码托管平台 | 直接把 data.json 写进仓库，大屏刷新即更新 | 已部署到 Gitee / GitHub 时 |

---

## 三、怎么让大屏“自动更新”（线上部署）

### 方式 A：手工上传（最稳，建议先跑通）

1. 把整个文件夹上传到 Gitee（码云）或 GitHub 仓库（公开仓库即可）。
2. 在仓库设置中开启 Pages 服务，得到一个网址，例如 `https://用户名.gitee.io/仓库名/`。
3. 以后管理员改完数据，点「导出 data.json」，在仓库里**覆盖上传**同名文件。
4. 一楼大屏的浏览器刷新（或设置定时刷新）即可看到最新进展。

> 提示：大屏首次部署后建议把网址加入收藏，并用“开机自动打开 + F11 全屏”的方式常驻。

### 方式 B：管理端一键发布（更省事）

在管理端点「发布到代码托管平台」，填写：

| 字段 | 说明 |
| --- | --- |
| 平台 | GitHub 或 Gitee |
| 用户名 / 组织 | 仓库所属账号 |
| 仓库名 | 存放本平台的仓库 |
| 文件路径 | 默认 `data.json` |
| 访问令牌 | 见下方获取方法 |

**令牌获取**
- Gitee：头像 → 设置 → 安全设置 → 私人令牌 → 生成新令牌，勾选 `projects` 权限。
- GitHub：Settings → Developer settings → Personal access tokens → Fine-grained tokens，仓库选择该仓库，权限 `Contents: Read and write`。

令牌只保存在本机浏览器里，不会发送到任何其他地方。发布成功后，大屏刷新即可看到更新。

> 注意：本地双击打开（`file://`）时浏览器不允许读取 `data.json`，大屏会自动使用内置的 `js/data.js`，这不影响使用；只有部署到服务器后才会走 `data.json` 这条自动更新通道。

---

## 四、关于“注册 + 超管审批 + 普通管理员”的说明

您描述的是**多人账号体系**（超管审批注册、普通管理员上传数据）。这需要一个后端服务来保存账号和权限，纯静态网页做不了（把密码写在前端等于没有密码）。

目前可用的三种做法：

1. **现状（已实现）**：管理端文件 + 操作口令，指定一名管理员使用。口令仅用于防止误操作，不做身份认证。
2. **推荐**：把大屏部署到 Gitee/GitHub Pages，管理端用「一键发布」，只需要管好令牌的发放即可——谁拿到令牌谁才能改，效果等同于“审批通过才给权限”。
3. **如确实需要多人注册审批**：可接入免费的云开发（腾讯云开发 / LeanCloud）做账号与角色，或用单位现有服务器部署一个小后端。**需要的话告诉我，我按这个路子再出一版。**

---

## 五、数据来源与口径

- 数据来源：《杜集经济开发区管委会项目工作专班推进清单》（表内日期：2026.09.22），共 9 个项目专班。
- 节点统计：9 个项目合计 **98 个手续节点**，已办结 **59 项**，办结率 **60.2%**。
- 项目状态自动判定规则（未手工填写“状态”时生效）：

| 条件 | 状态 |
| --- | --- |
| 已办结“入规” | 已入规 |
| 已办结“纳统” | 已投产 |
| 已办结“工程开工”或“设备进场” | 建设中 |
| 已办结“施工许可证” | 即将开工 |
| 已办结“立项备案” | 前期手续 |
| 已办结“投资协议签订” | 已签约 |

**两点请核对**

1. 金派克二期「环评手续」：台账“是否完成”一列填的是“否”，但“进展情况”写的是“已完成（2026年9月14日）”。本平台按**已完成**计入，请在管理端确认后统一。
2. 金石机器人项目除协议、注册、立项外其余节点台账为空，本平台按“待启动”显示。

**关于手机号**：仓库是公开的，网页源码任何人都能看。台账里的企业联系人以及节点负责人手机号已统一做脱敏处理（如 182****1506）。如需完整号码，请在管理端自己维护，或改为内部群/电话簿通知。

---

## 六、文件清单

```
智慧化管理平台/
├─ index.html            大屏主页（双击即可播放）
├─ admin.html            管理端（改数据、传照片、发布）
├─ css/main.css          大屏样式
├─ js/data.js            项目数据（可直接用记事本编辑）
├─ js/main.js            大屏逻辑（统计、轮播、联动）
├─ js/particles.js       粒子星链背景
├─ js/admin.js           管理端逻辑
├─ images/land-use-map.png   土地利用现状图（已重新配色、去白底）
├─ 效果预览.png          大屏效果图
└─ 使用说明.md          本文件
```

## 七、后续可扩展（按需再说）

- 项目现场照片按周更新，形成“进度影像”，大屏轮播。
- 增加“投资额 / 产值 / 用地面积”指标，配套柱状图、进度漏斗（数据补齐即可）。
- 增加“本周应办事项”提醒条，把临期节点标红预警。
- 手机端查看（同一份数据生成竖屏版本）。
